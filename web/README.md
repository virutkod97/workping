# WorkPing – Web

React + Vite + Ant Design. Khi chạy dev, `/api` được proxy sang `http://localhost:4000` (đổi bằng biến `API_URL`).

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # tạo dist/ — backend tự phục vụ thư mục này ở production
```
